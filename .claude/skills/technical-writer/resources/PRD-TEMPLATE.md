# 📋 PRD — `<project-name>` (Testing Contract)

> **Purpose of this document.** This is the **product requirements + testing contract** for
> `<project-name>`. It states *what the software must do* and *how to measure success* as
> **verifiable assertions**, so a human, an agent, or an autonomous tester can derive precise
> tests from it. It is **not** a usage manual.
>
> Generated by analyzing the project's own code (entry points, routes/controllers or
> routes/components, data models, existing tests). Any discrepancy found between the code and
> existing docs is noted below rather than silently resolved.

---

## 1. Overview & purpose

<!-- What this microservice is, in 1-2 paragraphs. Project kind: backend / frontend
(see scope-backend-vs-frontend). Note any code-vs-docs discrepancy found. -->

## 2. Scope

**In scope (this contract tests):** <!-- list -->

**Out of scope:** <!-- list — e.g. third-party services, manual-only browser behavior,
infrastructure/deployment -->

## 3. Functional requirements (FR)

Each FR is a *verifiable* statement with a stable id, cited by file path (and line where
useful). Group into subsections that match the project's own domain.

### 3.1 <category, e.g. Routing & validation>
- **FR-X1** <statement> (`path/to/file.ext:line`)

### 3.2 <category, e.g. Data model>
- **FR-X2** <statement> (`path/to/file.ext:line`)

<!-- add more subsections as needed -->

## 4. Non-functional requirements (NFR)

- **NFR-1** <e.g. exit code / HTTP status is the failure signal>
- **NFR-2** <e.g. no secrets/stack traces leaked in error responses>
- **NFR-3** <e.g. cross-platform / cross-browser behavior>

## 5. Success metrics

The product is "working" when **all** of these hold:
1. <metric, referencing FR ids>
2. <metric, referencing FR ids>

## 6. Test strategy — tiers

| Tier | Verifies | Hermetic? | Where it runs |
| ---- | -------- | --------- | -------------- |
| <e.g. Unit> | <what> | ✅/⚠️/❌ | <command/location> |
| <e.g. Integration> | <what> | | |
| <e.g. E2E / boot smoke> | <what> | | |
| <e.g. Manual / browser> | <what> | ❌ | manual |

## 7. Test data & execution flow

<!-- struct-test-flow: the records to seed before testing, and the ordered step-by-step endpoint
runbook. Derive both from the code read during analysis (reference fields → seed/create the
target first; auth guard → authenticate first; /:id routes → create before read/update/delete).
For a frontend project, express this as required app state / preceding navigation instead. -->

### 7.1 Preconditions & seed data

<!-- Records that must exist before any functional scenario runs. One row per seed record. -->

| Seed record | Model | How (seed endpoint/script → create endpoint → direct insert) | Values |
| ----------- | ----- | ------------------------------------------------------------ | ------ |
| | | | |

### 7.2 Ordered endpoint sequence

<!-- Numbered runbook. Each step: METHOD /path, what it consumes from an earlier step (seeded
reference, id from a create, auth cookie/token), what it produces for later steps. -->

1. **`<METHOD> <path>`** → <what it does; capture `<value>` for later steps>
2. **`<METHOD> <path>`** (uses `<value>` from step 1) → ...

## 8. Test scenarios (Given / When / Then)

| # | Given | When | Then | FR |
| - | ----- | ---- | ---- | -- |
| 1 | | | | |

## 9. How to run the tests

<!-- Actual commands if the project already has a test setup. If it doesn't yet, say so
explicitly instead of inventing scripts that don't exist. -->

```bash
<commands, if any>
```
